const slider: HTMLElement = document.querySelector('.slider');

const sliderContent: string =
'<div class="slider__handle-lower"></div>' +
  '<div class="slider__highlight"></div>' +
'<div class="slider__handle-upper"></div>';

slider.innerHTML = sliderContent;